# Modelfile for lfm-observer fine-tuned model

FROM ./lfm-observer.gguf

# System prompts will be provided at inference time for task differentiation
# No default system prompt needed

# Optimized parameters (from LFM2.5 docs)
PARAMETER temperature 0.1
PARAMETER top_p 0.1
PARAMETER top_k 50
